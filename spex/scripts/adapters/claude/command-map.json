{
  "harness": "claude",
  "version": "1.0.0",
  "inline": [
    {
      "neutral": "do NOT present interactive prompts",
      "adapted": "do NOT use AskUserQuestion"
    },
    {
      "neutral": "suppress all interactive prompts, complete the stamp",
      "adapted": "suppress all user prompts (do NOT use AskUserQuestion), complete the stamp"
    },
    {
      "neutral": "suppress all interactive prompts, complete the verification",
      "adapted": "suppress all user prompts (do NOT use AskUserQuestion), complete the verification"
    },
    {
      "neutral": "present the choice using a structured interactive prompt",
      "adapted": "present the choice using `AskUserQuestion`"
    },
    {
      "neutral": "using a structured interactive prompt (skip in autonomous mode)",
      "adapted": "using AskUserQuestion (skip in autonomous mode)"
    },
    {
      "neutral": "**This MUST be a structured interactive prompt, not a markdown text prompt.**",
      "adapted": "**This MUST be an AskUserQuestion tool call, not a markdown text prompt.**"
    },
    {
      "neutral": "Spawn a worker agent with the following prompt",
      "adapted": "Spawn a subagent using the Agent tool with the following prompt"
    },
    {
      "neutral": "spawn a fresh-context worker agent",
      "adapted": "spawn a fresh-context Agent (subagent_type: general-purpose)"
    },
    {
      "neutral": "a general-purpose worker agent",
      "adapted": "`subagent_type: \"general-purpose\"` via the Agent tool"
    },
    {
      "neutral": "Dispatch all review agents using multiple subagent calls in a single message",
      "adapted": "Dispatch all 5 agents using multiple Agent tool calls in a single message"
    },
    {
      "neutral": "the agent's subagent mechanism",
      "adapted": "the Agent tool"
    },
    {
      "neutral": "Use the agent's subagent mechanism with",
      "adapted": "Use the Agent tool with"
    },
    {
      "neutral": "The parallel agent teams feature must be enabled",
      "adapted": "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS must be set"
    },
    {
      "neutral": "Use the agent's team mechanism to spawn teammates",
      "adapted": "Use the **Agent** tool with `team_name` to spawn teammates in isolated worktrees"
    },
    {
      "neutral": "Use the agent's team mechanism to spawn research agents",
      "adapted": "Use the **Agent** tool with `team_name` to spawn research agents. Each agent explores in its own context"
    }
  ],
  "sections": {
    "parallel-dispatch": "Dispatch all agents using multiple **Agent tool** calls in a single message.\nEach agent runs in isolated context with `subagent_type: \"general-purpose\"`.\nSend all dispatch calls in one response for maximum parallelism.",
    "agent-teams": "Enable Claude Code Agent Teams by setting the feature flag:\n\n```bash\n# Check settings.local.json for the feature flag\nFLAG=$(jq -r '.env.CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS // \"\"' .claude/settings.local.json 2>/dev/null)\n```\n\n**If the flag is not set (`\"\"` or missing):**\n\n1. Set it in `.claude/settings.local.json`:\n   ```bash\n   jq '.env.CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS = \"1\"' .claude/settings.local.json > /tmp/settings.json && mv /tmp/settings.json .claude/settings.local.json\n   ```\n2. Inform the user: \"Agent Teams feature flag has been enabled. Please restart Claude Code for teams to activate.\"\n3. **Fall back to sequential execution** for this session (teams will work on next run).\n\n**If the flag is set:** Proceed with team orchestration.\n\n**If the flag becomes unset mid-session** (e.g., user restarts without it): The pre-flight check runs at skill invocation time, not continuously. If the env var disappears mid-session, already-spawned teammates continue working. On next invocation, the check will catch the missing flag and fall back to sequential.",
    "agent-teams-dispatch": "Use the **Agent** tool with `team_name` to spawn teammates in isolated worktrees.\n\nIf the current harness does not support parallel dispatch, execute tasks sequentially in the current session.",
    "agent-teams-research-dispatch": "Use the **Agent** tool with `team_name` to spawn research agents. Each agent explores in its own context.\n\nIf the current harness does not support parallel dispatch, research all topics sequentially.",
    "worktree-isolation": "**IMPORTANT:** Do NOT use the `ExitWorktree` tool (Claude Code only). Spex worktrees are created via `git worktree add`, not `EnterWorktree`, so `ExitWorktree` will refuse to operate on them. Always use git commands directly. On agents without `EnterWorktree` (Codex, OpenCode), worktrees are always managed via git commands."
  },
  "fallback_note": "> **Note:** This capability is not available on {harness}. {fallback_text}"
}
